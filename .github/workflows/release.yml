name: Release

# Push a version tag (e.g. v0.2.0) to build and stage a release:
#   * desktop installers — macOS (.dmg, signed + notarized), Windows (.exe),
#     Linux (.AppImage)
#   * the @aifight/aifight CLI tarball (.tgz)
# Everything is attached to a DRAFT GitHub Release so you can review + edit notes,
# then click Publish. Nothing goes public automatically.
#
# Trigger `workflow_dispatch` from the Actions tab to dry-run the builds without
# creating a release.
#
# Repo secrets used for signed macOS builds (Settings → Secrets and variables →
# Actions). They are scoped to the macOS runner only:
#   CSC_LINK, CSC_KEY_PASSWORD                              Developer ID cert (.p12 base64 + password)
#   APPLE_ID, APPLE_APP_SPECIFIC_PASSWORD, APPLE_TEAM_ID    notarization
# Windows builds are currently unsigned (users get a SmartScreen "More info →
# Run anyway" prompt). Linux needs no signing.

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Build only — do not create a release"
        type: boolean
        default: true

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  desktop:
    name: Desktop (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "22"
      - name: Install workspace deps (root)
        run: npm ci
      - name: Build CLI engine (desktop bundles it)
        run: npm run build:cli
      - name: Package desktop installer
        working-directory: desktop
        # Signing secrets are passed ONLY on macOS — CSC_LINK is an Apple
        # Developer ID cert and must not reach the Windows/Linux signers.
        # CSC_IDENTITY_AUTO_DISCOVERY=false keeps electron-builder from probing
        # the keychain; an explicit CSC_LINK still signs.
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
          CSC_LINK: ${{ matrix.os == 'macos-latest' && secrets.CSC_LINK || '' }}
          CSC_KEY_PASSWORD: ${{ matrix.os == 'macos-latest' && secrets.CSC_KEY_PASSWORD || '' }}
          APPLE_ID: ${{ matrix.os == 'macos-latest' && secrets.APPLE_ID || '' }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ matrix.os == 'macos-latest' && secrets.APPLE_APP_SPECIFIC_PASSWORD || '' }}
          APPLE_TEAM_ID: ${{ matrix.os == 'macos-latest' && secrets.APPLE_TEAM_ID || '' }}
        run: npm run package
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: desktop-${{ matrix.os }}
          path: |
            desktop/release/*.dmg
            desktop/release/*.zip
            desktop/release/*.exe
            desktop/release/*.AppImage
            desktop/release/*.blockmap
            desktop/release/latest*.yml
          if-no-files-found: ignore
          retention-days: 14

  cli:
    name: CLI tarball
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "22"
      - name: Install workspace deps (root)
        run: npm ci
      - name: Build CLI
        run: npm run build:cli
      - name: Pack tarball
        id: pack
        working-directory: runtime
        run: |
          set -e
          npm pack
          tarball=$(ls aifight-aifight-*.tgz | head -1)
          if [ -z "$tarball" ]; then
            echo "ERROR: npm pack produced no aifight-aifight-*.tgz" >&2
            exit 1
          fi
          echo "tarball=$tarball" >> "$GITHUB_OUTPUT"
          echo "packed: $tarball"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: cli-tarball
          path: runtime/${{ steps.pack.outputs.tarball }}
          if-no-files-found: error
          retention-days: 14

  release:
    name: Draft GitHub Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [desktop, cli]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          path: artifacts
      - name: Collect release files
        run: |
          set -e
          mkdir -p dist
          find artifacts -type f \( \
            -name '*.dmg' -o -name '*.zip' -o -name '*.exe' -o \
            -name '*.AppImage' -o -name '*.tgz' -o \
            -name '*.blockmap' -o -name 'latest*.yml' \) \
            -exec cp -n {} dist/ \;
          echo "Release files:"
          ls -la dist
      - name: Create draft release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
        with:
          draft: true
          prerelease: ${{ contains(github.ref_name, '-alpha') || contains(github.ref_name, '-beta') || contains(github.ref_name, '-rc') }}
          name: ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
          fail_on_unmatched_files: false
          files: dist/*
          body: |
            AIFight client ${{ github.ref_name }}.

            ## Downloads

            | Platform | File | Notes |
            |---|---|---|
            | macOS | `*.dmg` | signed & notarized |
            | Windows | `*.exe` | unsigned — on first run click **More info → Run anyway** |
            | Linux | `*.AppImage` | `chmod +x` then run |
            | CLI | `npm install -g @aifight/aifight` | or the `*.tgz` attached here |

            Bring your own LLM — your API key stays on your machine and never
            touches our servers.
